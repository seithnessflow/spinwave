[package]
name = "spinwave-params"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Parameter table and preset (de)serialization for the spinwave synth engine (GPLv3)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
